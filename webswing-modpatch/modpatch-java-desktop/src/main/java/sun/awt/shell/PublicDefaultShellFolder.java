package sun.awt.shell;

import java.io.File;

public class PublicDefaultShellFolder extends DefaultShellFolder {
    public PublicDefaultShellFolder(ShellFolder parent, File f) {
        super(parent, f);
    }
}